---
---
<h2>Resumen</h2>
<div class="summary-grid">
  <a href="/menu" class="summary-card">
    <h3>Nuestro Menú</h3>
    <p>Explora desayunos, almuerzos y opciones saludables.</p>
  </a>
  <a href="/delivery" class="summary-card">
    <h3>Zona de Reparto</h3>
    <p>Consulta nuestras coberturas y precios de envío.</p>
  </a>
  <a href="/nosotros" class="summary-card">
    <h3>Nuestra Historia</h3>
    <p>Conoce la misión y valores que nos impulsan.</p>
  </a>
</div>

<style>
  .summary-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
  }

  .summary-card {
    background: var(--white);
    padding: 1.5rem;
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    text-align: center;
    cursor: pointer;
    transition: var(--transition);
    border: 2px solid transparent;
    text-decoration: none;
    color: inherit;
    display: block;
  }

  .summary-card:hover {
    border-color: var(--primary-color);
    transform: translateY(-5px);
  }

  .summary-card h3 {
    color: var(--primary-dark);
    margin-bottom: 0.5rem;
  }

  .summary-card p {
    color: var(--text-light);
  }
</style>
